% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{JBU_filter}
\alias{JBU_filter}
\title{Select predictors for each series}
\usage{
JBU_filter(X_block, y_mx, K)
}
\arguments{
\item{X_block}{A numeric matrix of candidate predictors.}

\item{y_mx}{A numeric matrix of observed series. Rows are time points and
columns are series.}

\item{K}{An integer giving the number of predictors to select for each series.}
}
\value{
An integer matrix with \code{K} rows and one column for each series in
\code{y_mx}. Each column contains the selected predictor indices.
}
\description{
Applies the JBU filtering step to select the \code{K} most relevant predictors
from \code{X_block} for each series in \code{y_mx}.
}
\examples{
set.seed(1)
X_block <- matrix(rnorm(40), nrow = 10, ncol = 4)
y_mx <- cbind(
  2 * X_block[, 1] + rnorm(10, sd = 0.1),
  -1 * X_block[, 3] + rnorm(10, sd = 0.1)
)

JBU_filter(X_block, y_mx, K = 2)

}
