% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{JBU_X_alpha}
\alias{JBU_X_alpha}
\title{Build the filtered predictor matrix}
\usage{
JBU_X_alpha(X_block, y_mx, K)
}
\arguments{
\item{X_block}{A numeric matrix of candidate predictors.}

\item{y_mx}{A numeric matrix of observed series. Rows are time points and
columns are series.}

\item{K}{An integer giving the number of predictors to select for each series.}
}
\value{
A numeric matrix containing the filtered block-diagonal predictor
structure. If \code{X_block} has \code{T} rows, \code{y_mx} has \code{M} columns, and \code{K}
predictors are selected per series, the result has \code{T * M} rows and
\code{K * M} columns.
}
\description{
Applies the JBU filter and constructs the block-diagonal predictor matrix
used by the sampler.
}
\examples{
set.seed(1)
X_block <- matrix(rnorm(40), nrow = 10, ncol = 4)
y_mx <- cbind(
  2 * X_block[, 1] + rnorm(10, sd = 0.1),
  -1 * X_block[, 3] + rnorm(10, sd = 0.1)
)

X_alpha <- JBU_X_alpha(X_block, y_mx, K = 2)
dim(X_alpha)
X_alpha

}
